# Copy to .env and fill in. .env is gitignored — keep it that way.

# --- database ---
POSTGRES_DB=baydo
POSTGRES_USER=baydo
# Generate one: openssl rand -base64 32
POSTGRES_PASSWORD=

# --- api ---
SESSION_HOURS=12

# --- web ---
WEB_PORT=8080
PUBLIC_URL=http://localhost:8080

# --- optional: AI drafting. Without it, drafts fall back to manual. ---
ANTHROPIC_API_KEY=
